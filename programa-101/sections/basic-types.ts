// Strings
export let firstName: string = 'Josué';
let lastName: string = 'Camacho';

console.log(firstName, lastName);

// Darle nombres significativos a las variables

let fullName: string = firstName + ' ' + lastName;

console.log(fullName);

// Numeros
let number1: number = 5;
let number2: number = 10;

let sum: number = number1 + number2;
let sub: number = number1 - number2;

console.log('sum:', sum);
console.log('difference:', sub);

// Booleanos
let isActive: boolean = true;
let isRunning: boolean = false;
let hasKids: boolean = true;

console.log('isActive:', !isActive);
console.log('isRunning:', isRunning);
console.log('hasKids:', hasKids);
