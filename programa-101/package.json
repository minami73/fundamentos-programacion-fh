{
	"name": "programa-101",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"dev": "ts-node app.ts",
		"dev:watch": "nodemon --watch . --ext ts --exec ts-node app.ts",
        
        "// comentario": "Anteriormente el dev:watch estaba como",
        "// anterior_script": "nodemon --watch '-/**/*.ts' --exec 'ts-node' app.ts",
        "// comentario2": "porque fh lo configuró para Mac/Linux, pero en Windows se trata diferente"
	}, 
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/node": "^18.6.2",
		"nodemon": "^2.0.19",
		"ts-node": "^10.9.1",
		"tslib": "^2.4.0",
		"typescript": "^4.7.4"
	}
}